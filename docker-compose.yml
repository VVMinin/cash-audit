version: '3.9'

services:
  backend:
    build: ./backend
    env_file:
      - ./backend/.env
    restart: always
    ports:
      - "5000:5000"

  frontend:
    build: ./frontend
    restart: always
    ports:
      - "80:80"
    depends_on:
      - backend

